SDL-based virtual framebuffer device
====================================

This is a CMRX driver which provides virtual framebuffer device. Its API is exposed via RPC interfaces so upper layers don't care where the output goes.
